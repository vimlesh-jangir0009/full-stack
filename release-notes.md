# Release Notes

## 18-03-2025  v1.0.0 - Initial Setup (Node & React)

### Added
- Initialized a Node.js project.
- Set up React.js with Vite.
- Installed required dependencies:
  - `react`, `react-dom`
  - `vite` for fast development
  - `eslint` and `prettier` for code linting and formatting

### How to Install
1. Clone the repository:
   ```sh
   git clone <https://github.com/vimlesh-jangir0009/full-stack>
   cd <full-stack>
   ```
2. Install dependencies:
   ```sh
   npm install
   ```
3. Start the development server:
   ```sh
   npm run dev
   ```

### Notes
- Ensure Node.js (LTS version) is installed.
- Use `.env` file for environment variables.
- Follow the project structure guidelines for better maintainability.

---

## Upcoming Features
- Add Redux for state management.
- Implement React Router for navigation.
- Set up testing with Jest and React Testing Library.

Stay tuned for updates! 🚀